<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rakesh_ap2016.hw2_rakeshne">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/layoutlauncher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LinearLayout" />
        <activity android:name=".GridLayout" />
        <activity android:theme="@style/myTheme" android:name=".CalculatorLayout" />
        <activity android:theme="@style/myTheme" android:name=".MovieImage"></activity>
    </application>

</manifest>
